// Phase G: ai_controller removed from TerminalView struct; tests requiring it are disabled.
// These tests will be re-enabled or removed in Phase F when the controller layer is deleted.

#[test]
#[ignore = "Phase G: ai_controller removed from TerminalView struct"]
fn passive_suggestions_request_params_omit_ambient_agent_task_id() {
    // test body disabled — see above
}
